# Environment variables for RiskRadar
# (Currently none required for v1.0 — internal only, no auth)

# Future use (v1.1+):
# LOG_LEVEL=INFO
# API_PORT=8000
# CONFIG_PATH=backend/config/scoring_weights.yaml
